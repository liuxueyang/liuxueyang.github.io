#include <cstdio>
#include <algorithm>

using namespace std;

const int N = 1000010, INF = 1e9 + 10;
int n, m, a[N];

int bsearch(ll l, ll r) {
  ll mid;
  while (l < r) {
    
  }
}

int main() {
  scanf("%d%d", &n, &m);
  for (int i = 1; i <= n; ++i) scanf("%d", a + i);
  
  return 0;
}
